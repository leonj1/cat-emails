# Gmail Categorizer Environment Variables

# Gmail credentials
# Use a Gmail App Password (not your regular Gmail password).
# - Enable 2-Step Verification, then create an App Password at: https://myaccount.google.com/apppasswords
# - The App Password is 16 characters. Do NOT include spaces. If Google shows it with spaces, paste it without spaces.
GMAIL_EMAIL=your-email@gmail.com
GMAIL_PASSWORD=your-app-password

# Control API credentials
CONTROL_TOKEN=your-api-token

# RequestYAI/OpenAI-compatible LLM settings
# Provide the full OpenAI-compatible API root (often includes version path).
# Example (RequestYAI): https://router.requesty.ai/v1
REQUESTYAI_BASE_URL=https://router.requesty.ai/v1
REQUESTYAI_API_KEY=your-requestyai-api-key
# Legacy names also supported for compatibility:
REQUESTY_API_URL=https://router.requesty.ai/v1
REQUESTY_API_KEY=your-requestyai-api-key

# Optional settings
HOURS=2  # Number of hours to look back for emails

# Note: Replace the values above with your actual Gmail credentials and other settings
# Do not commit the actual .env file with real credentials to version control

# Mailtrap API credentials (for API-based email sending)
MAILTRAP_KEY=your_key

# SMTP credentials for mailfrom.dev
SMTP_USERNAME=your-smtp-username
SMTP_PASSWORD=your-smtp-password

# Email summary settings
ENABLE_SUMMARIES=true  # Set to false to disable summary reports
SUMMARY_RECIPIENT_EMAIL=your-email@gmail.com  # Who receives the summary reports

# Database configuration
# Choose ONE of the following database configurations:

# Option 1: SQLite (local file-based database)
# Path to the SQLite database file used by the app.
# - For local runs: can be relative (default below)
# - For Docker: set to an absolute path inside the container (e.g., /app/email_summaries/summaries.db)
DATABASE_PATH=./email_summaries/summaries.db

# Option 2: MySQL (local or cloud-hosted)
# Individual connection parameters (recommended for clarity)
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DATABASE=cat_emails
MYSQL_USER=cat_emails_user
MYSQL_PASSWORD=your-mysql-password

# OR use a full MySQL connection string:
# MYSQL_URL=mysql+pymysql://user:password@host:port/database

# MySQL connection pool settings (optional, defaults shown)
# MYSQL_POOL_SIZE=5
# MYSQL_MAX_OVERFLOW=10
# MYSQL_POOL_RECYCLE=3600

