# Requirements for Class Refactoring Agent System

# Core dependencies
claude-agent-sdk>=0.1.0

# CLI and output formatting
rich>=13.0.0

# Environment variables
python-dotenv>=1.0.0

# Async support
nest-asyncio>=1.5.0

# Testing framework (for running generated tests)
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-mock>=3.10.0

# Code analysis (already in standard library, but listing for reference)
# ast (built-in)
# hashlib (built-in)
# subprocess (built-in)
